{
  "name": "secondlook-ai",
  "version": "0.1.0",
  "private": true,
  "description": "SecondLook AI gateway — Cloudflare Worker. Same backend logic as Pitchwire, separate deployment and task set.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^3.90.0"
  }
}
